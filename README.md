# little-west-twelveth

# Setup

```
$ git clone 
$ cd little-west-twelveth
$ npm init
      // main: server.js
$ npm install
$ npm install node-static --save
```

Create the server, refer: http://www.kevinleary.net/simple-node-static-server/
